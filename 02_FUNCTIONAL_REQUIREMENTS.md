# المتطلبات الوظيفية التفصيلية

تستخدم الرموز `FR-xxx` في الاختبارات ومصفوفة القبول. كل متطلب هنا ملزم ما لم يُذكر أنه اختياري.

## A. الهوية والوصول

- `FR-001` يدعم النظام تسجيل الدخول بالبريد وكلمة المرور وجلسة آمنة عبر Cookie من الخادم.
- `FR-002` يستطيع المستخدم طلب رابط استعادة كلمة المرور؛ وإذا لم يُضبط مزود البريد، ينشئ النظام رابطًا محدود المدة ينسخه المسؤول من لوحة الإدارة.
- `FR-003` يستطيع مسؤول مساحة العمل دعوة مستخدم بالبريد أو إنشاء رابط/رمز دعوة محدود الصلاحية وعدد الاستخدامات.
- `FR-004` يُلزم النظام تغيير كلمة المرور المؤقتة في أول دخول للحسابات التي أنشأها المسؤول.
- `FR-005` يستطيع المستخدم رؤية الجلسات النشطة وتسجيل الخروج من الجلسات الأخرى.
- `FR-006` يدعم تعليق المستخدم أو إلغاء عضويته دون حذف مخرجاته التاريخية.
- `FR-007` لا يكشف خطأ الدخول إن كان البريد مسجلًا أم لا.

## B. مساحات العمل والإعدادات

- `FR-010` ينشئ مدير المنصة مساحة عمل، ويحدد اسمها وشعارها وألوانها والمنطقة الزمنية واللغة وسياسة الاحتفاظ.
- `FR-011` لكل مساحة عمل مفتاح URL قصير فريد، وأعضاء وأدوار وبيانات معزولة.
- `FR-012` يستطيع مالك المساحة نقل الملكية بعد إعادة التحقق من الهوية.
- `FR-013` يستطيع المالك تصدير جميع بيانات المساحة بصيغة JSON منظمة وCSV للجداول الرئيسية.
- `FR-014` حذف المساحة عملية مؤجلة ومؤكدة، مع فترة استعادة يحددها التنفيذ ولا تقل عن 7 أيام.

## C. المنظمات والبرامج

- `FR-020` يدير المخولون ملف منظمة: الاسم، الاسم المختصر، المنطقة، المدينة، المجالات، الوصف، جهة الاتصال، والملاحظات الداخلية.
- `FR-021` يمكن ربط مستخدم بمنظمة واحدة أو أكثر مع صفته فيها.
- `FR-022` يمكن استيراد المنظمات من CSV بقالب تنزيل، ومعاينة الأخطاء قبل الحفظ.
- `FR-023` يمكن دمج سجلين مكررين بعد إظهار الآثار، مع إعادة ربط العضويات والمشاركات.
- `FR-024` ينظم البرنامج مجموعة ورش وله اسم ومالك وفترة وأهداف وحالة.
- `FR-025` يمكن أرشفة منظمة أو برنامج دون كسر التقارير التاريخية.

## D. إدارة الورشة

- `FR-030` ينشئ المخول ورشة من الصفر أو قالب أو نسخة من ورشة سابقة.
- `FR-031` تشمل الورشة: العنوان، الوصف، الصورة، البرنامج، النمط، المكان/الرابط، المنطقة الزمنية، السعة، التسجيل، التواريخ، الأهداف، المحاور، والجهات المنظمة.
- `FR-032` حالات الورشة: مسودة، التسجيل مفتوح، مجدولة، مباشرة، متوقفة مؤقتًا، مكتملة، مؤرشفة.
- `FR-033` يتحقق الخادم من انتقالات الحالة؛ لا يمكن بدء ورشة بلا يوم أو بند أجندة أو ميسر.
- `FR-034` يستطيع المستخدم المخول معاينة الورشة كما يراها المشارك قبل النشر.
- `FR-035` يحفظ سجل نسخ للنصوص والإعدادات المهمة أو يسجل تغييراتها في سجل التدقيق.
- `FR-036` يمكن تحديد متطلبات قبل الورشة وبعدها مع موعد وحالة إلزام.
- `FR-037` يمكن ربط ملفات ومواد وروابط بالورشة أو بند أجندة مع مستوى ظهور.
- `FR-038` يمكن نسخ الورشة دون نسخ المشاركين والإجابات والحضور؛ ويمكن اختيار نسخ المنظمات/المجموعات كهيكل فقط.

## E. الأيام والأجندة

- `FR-040` يدعم منشئ الأجندة يومًا أو عدة أيام وبنودًا مرتبة.
- `FR-041` أنواع بند الأجندة: افتتاح، عرض، نقاش، نشاط، استراحة، عرض مجموعات، إغلاق، ومخصص.
- `FR-042` يرتبط البند اختياريًا بنشاط ومواد وميسر.
- `FR-043` يمنع التداخل الزمني أو يعرض تحذيرًا صريحًا يمكن للمخول تجاوزه مع سبب.
- `FR-044` يظهر للمشارك الوقت المحلي والعنوان والحالة، ويُبرز البند الحالي.
- `FR-045` يمكن تصدير الأجندة وطباعتها ومشاركتها برابط وفق مستوى الخصوصية.

## F. التسجيل والدعوات والحضور

- `FR-050` التسجيل إما مغلق، بالدعوة، أو عام مع اعتماد تلقائي/يدوي.
- `FR-051` يحدد منشئ الورشة حقول التسجيل المطلوبة من مجموعة حقول آمنة قابلة للتخصيص.
- `FR-052` يكتشف النظام التسجيل المكرر بالبريد داخل الورشة ويعرض مسار دمج آمن.
- `FR-053` يستطيع المنسق قبول أو رفض أو وضع الطلب في قائمة انتظار مع سبب داخلي اختياري.
- `FR-054` ينتج لكل مشارك رمز حضور QR ورمزًا قصيرًا بديلًا.
- `FR-055` يسجل الحضور يدويًا أو عبر مسح QR، لكل يوم وبند اختياريًا، مع وقت وطريقة ومن سجله.
- `FR-056` يستطيع المخول تصحيح الحضور مع سبب؛ ويُسجل التعديل في التدقيق.
- `FR-057` تدعم قائمة المشاركين البحث والفلترة والتصدير وإرسال تذكير جماعي دون كشف عناوينهم لبعضهم.

## G. المجموعات

- `FR-060` ينشئ الميسر مجموعات يدويًا أو تلقائيًا حسب العدد/المنظمة/حقل تصنيف.
- `FR-061` يمكن تثبيت قائد أو مقرر للمجموعة، مع السماح لأي عضو أو عضو واحد بتحرير المخرج حسب إعداد النشاط.
- `FR-062` يمكن نقل مشارك بين المجموعات قبل النشاط أو أثناءه مع توضيح أثر النقل على التسليمات القائمة.
- `FR-063` يمكن استنساخ هيكل مجموعات لنشاط آخر أو إنشاء مجموعات مختلفة لكل نشاط.
- `FR-064` تعرض لوحة الميسر توازن المجموعات وحالة حضور أعضائها.

## H. محرك الأنشطة والاستبيانات

- `FR-070` ينشئ المخول نشاطًا من الأنواع: استبيان، اختبار، تصويت، تمرين فردي، تمرين جماعي، تقييم ذاتي، تقييم ميسر، التزام، ونموذج عام.
- `FR-071` يدعم الحقول: نص قصير/طويل، رقم، مقياس، اختيار واحد/متعدد، نعم/لا، ترتيب، مصفوفة، تاريخ/وقت، رابط، وملف.
- `FR-072` لكل حقل عنوان ووصف وإلزام وترتيب وقواعد تحقق وخيارات ومنطق ظهور شرطي بسيط.
- `FR-073` يمكن حفظ النشاط كمسودة، نشره، فتحه، إيقافه، إغلاقه، وإعادة فتحه بصلاحية.
- `FR-074` يحدد الجمهور: جميع المشاركين، منظمات، مجموعات، أو أفراد محددون.
- `FR-075` يحدد نمط الإجابة: لكل فرد، لكل منظمة، أو لكل مجموعة.
- `FR-076` يحفظ المشارك مسودة تلقائيًا محليًا وعلى الخادم عند الاتصال، ويرى آخر وقت مزامنة.
- `FR-077` يمنع النظام الكتابة الصامتة فوق إصدار أحدث؛ يستخدم رقم إصدار ويعرض حل تعارض مفهومًا.
- `FR-078` يمكن للنشاط قبول أكثر من محاولة، أو محاولة واحدة، أو السماح بالتعديل حتى الإغلاق.
- `FR-079` يدعم إخفاء أسماء المستجيبين عن الميسر في الأنشطة التي يحددها المصمم، مع منع إعادة التعريف من التقارير العادية.
- `FR-080` للاختبار إجابات صحيحة وأوزان وتغذية راجعة، ولا تظهر مفاتيح الإجابة قبل الإغلاق.
- `FR-081` يمكن ربط سؤال قبلي بنظيره البعدي وحساب الفرق الفردي والمجمع.
- `FR-082` يدعم التقييم Rubric بمعايير ومستويات وأوزان وتعليق.
- `FR-083` يمكن تنزيل مرفق فقط لمن يملك الوصول إلى الورشة/التسليم.
- `FR-084` يمكن تصدير بنية نشاط واستيرادها JSON بعد التحقق.

## I. التشغيل المباشر

- `FR-090` تعرض لوحة الميسر البند الحالي، النشاط الحالي، المؤقت، عدد المتصلين، وعدد المسودات/المكتملين.
- `FR-091` يستطيع الميسر فتح النشاط وإغلاقه وتمديد وقته وإرسال تنبيه للمشاركين.
- `FR-092` تظهر تغييرات الحالة للمشاركين خلال مدة مستهدفة لا تتجاوز 5 ثوانٍ في اتصال طبيعي.
- `FR-093` يمكن عرض نتائج مجمعة آمنة على شاشة العرض، مع استبعاد النصوص الحساسة أو الأسماء وفق الإعداد.
- `FR-094` يمكن إخفاء/إظهار نتيجة أو مخرج محدد من شاشة العرض.
- `FR-095` عند انقطاع الميسر لا تتلف الحالة؛ يستطيع ميسر آخر مخول متابعة الجلسة.
- `FR-096` يسجل النظام أحداث التشغيل المهمة: فتح/إغلاق/تمديد/تغيير البند.

## J. غرفة المشارك

- `FR-100` يرى المشارك لوحة واحدة للورش القادمة والحالية والمكتملة.
- `FR-101` داخل الورشة يرى الحالة الحالية، الأجندة، نشاطه المطلوب، مجموعته، الموارد، ونسبة الإكمال.
- `FR-102` لا يرى المشارك أدوات إدارية أو بيانات مشاركين لا يحتاجها.
- `FR-103` يستطيع المشارك تعديل ملفه الأساسي وتفضيلات اللغة والإشعارات.
- `FR-104` يستطيع الوصول لمخرجاته وشهادته بعد الورشة وفق سياسة الورشة.
- `FR-105` تظهر تعليمات واضحة عند عدم الاتصال، ولا يُعرض نجاح الإرسال قبل تأكيد الخادم.

## K. التقارير والتحليلات

- `FR-110` لوحة الورشة تعرض التسجيل والحضور والمشاركة وإكمال الأنشطة والتقييم العام.
- `FR-111` يمكن الفلترة حسب المنظمة والمجموعة واليوم والنشاط وحالة المشارك.
- `FR-112` يعرض تقرير القبلي/البعدي العدد والمتوسط والتوزيع والفرق، مع توضيح عدد الإجابات الناقصة.
- `FR-113` يعرض تقرير المخرجات نصوص وتمارين كل منظمة/مجموعة مع تقييم الميسر.
- `FR-114` يولد النظام تقريرًا ختاميًا قابلًا للطباعة يتضمن ملخص التنفيذ والأهداف والأجندة والحضور والمشاركة والنتائج والمخرجات والتوصيات.
- `FR-115` يمكن للمخول تحرير السرد والتوصيات في التقرير دون تغيير البيانات المحسوبة.
- `FR-116` تصدير CSV يستخدم UTF-8 BOM وعناوين عربية، وتصدير JSON يحافظ على المعرفات والعلاقات.
- `FR-117` التقارير المجمعة لا تعرض هوية إجابة مجهولة، ولا تسمح بفلاتر تنتج مجموعات صغيرة تكشفها؛ الحد الأدنى الافتراضي 5.

## L. الشهادات

- `FR-120` ينشئ المسؤول قالب شهادة باسم الورشة والمشارك والتاريخ والمدة وشعار المساحة ورمز تحقق.
- `FR-121` يحدد معيار الاستحقاق: حضور نسبة معينة، إكمال أنشطة، أو اعتماد يدوي.
- `FR-122` إصدار الشهادات فردي أو جماعي، مع منع الازدواج.
- `FR-123` صفحة تحقق عامة تعرض صحة الشهادة والاسم والعنوان والتاريخ فقط، ويمكن إلغاء الشهادة مع بقاء سجلها.
- `FR-124` تكون الشهادة صفحة HTML مضبوطة للطباعة إلى PDF دون خدمة خارجية.

## M. الإشعارات

- `FR-130` ينشئ النظام إشعارات داخلية للدعوة والتذكير وفتح النشاط والتغييرات المهمة وإصدار الشهادة.
- `FR-131` يستطيع المستخدم تعليم الإشعار كمقروء أو إيقاف فئات غير حرجة.
- `FR-132` تدعم التذكيرات المجدولة قبل الورشة وقبل موعد المتطلبات.
- `FR-133` يطبق مزود البريد عبر واجهة Adapter؛ فشل البريد لا يفقد الإشعار الداخلي ويظهر للمسؤول لإعادة المحاولة.

## N. الملفات والمواد

- `FR-140` يرفع المخول ملفات إلى R2 خاص بعد فحص الامتداد والحجم والتوقيع السحري الأساسي.
- `FR-141` يسمح الإصدار الأول بأنواع آمنة محددة: PDF، DOCX، XLSX، PPTX، PNG، JPG، WebP، TXT، CSV، وبحد افتراضي 20MB قابل للتقليل.
- `FR-142` يحفظ النظام الاسم الأصلي ونوع MIME والحجم والبصمة والمالك والغرض، ويستخدم مفتاح تخزين غير قابل للتخمين.
- `FR-143` الحذف منطقي أولًا؛ مهمة تنظيف تحذف الكائن غير المرتبط بعد فترة احتفاظ.

## O. الإدارة والتدقيق

- `FR-150` لوحة مدير المنصة تدير مساحات العمل والحسابات المعلقة والحالة العامة فقط؛ لا تدخل محتوى مساحة دون إجراء دعم مسجل.
- `FR-151` يسجل التدقيق: الفاعل، الفعل، نوع المورد، معرفه، المساحة، وقت UTC، IP مختصر/مموه، وكيل المستخدم، وملخص تغيير منزوع الأسرار.
- `FR-152` لا يمكن تعديل سجل التدقيق من واجهة التطبيق.
- `FR-153` توجد صفحة صحة تعرض حالة API والنسخة دون كشف الأسرار أو تفاصيل البنية.
- `FR-154` تعرض الواجهة معرف طلب قابل للمشاركة عند خطأ غير متوقع.

## P. القوالب والاستنساخ

- `FR-160` يمكن حفظ ورشة مكتملة أو مسودة كقالب باسم وتصنيف ووصف.
- `FR-161` يحتوي القالب على إعدادات الورشة والأهداف والمحاور والأجندة والأنشطة والـRubrics والمواد القابلة للنسخ، ولا يحتوي المشاركين أو الإجابات أو الحضور.
- `FR-162` يمكن إنشاء نسخة جديدة من القالب ثم تعديلها دون التأثير على الأصل.
- `FR-163` إصدار القالب محفوظ؛ تحديث القالب لا يغير الورش المنشأة منه.

## Q. البحث وقابلية الاستخدام

- `FR-170` يوجد بحث موحد ضمن مساحة العمل للورش والبرامج والمنظمات والمستخدمين حسب الصلاحية.
- `FR-171` تدعم القوائم الترتيب والفلاتر والترقيم الصفحي وحفظ الفلاتر الشائعة في الرابط.
- `FR-172` جميع الإجراءات الخطرة تطلب تأكيدًا يذكر المورد والأثر، ولا تعتمد اللون وحده.
- `FR-173` تعرض النماذج أخطاء قريبة من الحقول وتحافظ على المدخلات غير الحساسة عند فشل الطلب.
