# الأمن والخصوصية والتشغيل

## 1. نموذج التهديد المختصر

الأصول التي يجب حمايتها: حسابات المستخدمين، قوائم المشاركين، الإجابات والمخرجات، الملفات، بيانات الحضور، الشهادات، وأسرار الخدمات. التهديدات الأساسية: تجاوز صلاحيات بين المساحات، تخمين روابط الدعوة والشهادات، سرقة الجلسة، CSRF/XSS، إساءة الرفع، كشف بيانات مجهولة عبر التقارير، وتغييرات إدارية بلا أثر تدقيقي.

## 2. المصادقة والجلسات

- Better Auth مع Drizzle/D1، وبريد وكلمة مرور في الإصدار الأول.
- Cookies باسم آمن، `HttpOnly`, `Secure`, `SameSite=Lax` أو أشد حيث يسمح التدفق، ونطاق المضيف فقط.
- مدة جلسة افتراضية 7 أيام مع تدوير آمن وإبطال من الخادم؛ جلسات الإدارة الحساسة تتطلب إعادة تحقق.
- كلمات المرور لا تُخزن أو تسجل؛ تستخدم آلية المكتبة المدققة وإعداداتها الموصى بها في النسخة المثبتة.
- سر المصادقة لا يقل عن 32 بايت عشوائيًا ويخزن عبر `wrangler secret`.
- Rate limit للدخول والاستعادة والدعوات، ورسائل أخطاء لا تكشف وجود البريد.
- تفعيل MFA توسعة موصى بها بعد الإصدار الأول، لكن البنية لا تمنعها.

## 3. التفويض والعزل

- Deny by default: كل route خاص يحتاج permission معلنًا.
- لا يمرر العميل دورًا موثوقًا في الطلب.
- اختبار IDOR إلزامي لكل مورد تابع لمساحة/ورشة.
- استعلامات Drizzle مهيأة؛ لا SQL مبني من قيم مستخدم.
- الحقول القابلة للترتيب والفلترة تُختار من allowlist.
- جلسة دعم مدير المنصة مؤقتة ومبررة ويظهر شريط واضح أثناءها.

## 4. حماية الويب

- Hono Secure Headers مع CSP فعالة، ومنع inline scripts غير المصرح بها.
- CSRF لطلبات تغيير الحالة المعتمدة على Cookie، والتحقق من `Origin` للمسارات الحساسة.
- CORS مغلق على نفس الأصل في الإنتاج؛ لا `*` مع credentials.
- React يعرض النصوص كبيانات؛ يمنع HTML من المستخدم، وأي Markdown يُنقّى بقائمة سماح.
- Turnstile على التسجيل العام واستعادة الحساب بعد التحقق من الخادم؛ التحقق من الرمز إلزامي ولا يعتمد الواجهة فقط.
- Content-Type وAccept وbody size limits، مع `413` واضح للرفع الكبير.

## 5. الملفات

- R2 bucket خاص بلا وصول عام.
- الرفع عبر Worker أو URL محدود موقّع وفق قرار التنفيذ، وبعد إنشاء سجل رفع مصرح.
- التحقق من الحجم والامتداد وMIME وmagic bytes للأنواع المدعومة؛ تغيير الاسم لا يكفي.
- اسم R2 عشوائي؛ الاسم الأصلي metadata فقط ويُنقّى عند Content-Disposition.
- التنزيل عبر endpoint يثبت الصلاحية ويصدر استجابة قصيرة الصلاحية.
- لا تُعرض ملفات Office داخل DOM؛ تنزيل فقط. الصور تُخدم مع CSP و`X-Content-Type-Options: nosniff`.
- حذف الكائنات غير المرتبطة مهمة مجدولة مع سجل نتائج.

## 6. الخصوصية

- إشعار خصوصية قصير عند التسجيل يوضح الغرض والمدة ومن يطلع.
- حقول التسجيل الافتراضية: الاسم، البريد، المنظمة، الصفة؛ الهاتف اختياري ولا يُطلب دون غرض.
- لا تُجمع بيانات مستفيدين حقيقية في Seed أو أنشطة الورشة.
- إعداد احتفاظ لكل مساحة: افتراضي 24 شهرًا لبيانات الورش، مع إمكانية أقصر؛ التدقيق يحتفظ وفق سياسة منفصلة.
- التصدير محمي بإذن، ويُسجل في التدقيق، وتظهر تحذيرات عند احتوائه بيانات شخصية.
- الأنشطة المجهولة تُجمع دون user_id، وتطبق عتبة عرض مجمعة افتراضية 5.
- سجلات التطبيق لا تحتوي أجسام الإجابات أو كلمات المرور أو tokens أو رؤوس Cookie.

## 7. التدقيق

الأحداث الملزمة: الدخول الإداري الفاشل المتكرر، تغيير دور، دعوة، قبول/رفض تسجيل، تعديل حضور، نشر/إغلاق نشاط، كشف استجابة مخفية، تصدير، إصدار/إلغاء شهادة، تغيير سياسة احتفاظ، جلسة دعم، وحذف/استعادة.

سجل التغيير يحفظ أسماء الحقول المتغيرة والقيم غير الحساسة المختصرة؛ القيم الحساسة تُمثل بـ`[REDACTED]`.

## 8. النسخ الاحتياطي والاستعادة

- تستخدم D1 Time Travel كنقطة استعادة أساسية؛ في الخطة المجانية يكون سجل الاستعادة المتاح أقصر من المدفوعة، لذلك توثق الخطة المختارة قبل الإطلاق.
- تصدير دوري مشفر إلى R2 للاحتفاظ الأطول، مع سياسة lifecycle ونسخة شهرية تحققية.
- الملفات في R2 لها فهرس D1 وبصمة؛ فحص شهري يكتشف الكائنات المفقودة أو اليتيمة.
- تجربة استعادة ربع سنوية في بيئة منفصلة، مع تسجيل RPO/RTO الفعليين.
- الهدف الأولي: RPO ≤ 24 ساعة للتصدير الطويل، مع الاستفادة من Time Travel للحوادث الأقرب؛ RTO ≤ 4 ساعات لحجم الإصدار الأول.
- استعادة D1 فوق الإنتاج إجراء مدمر لا ينفذه التطبيق؛ تتم يدويًا بواسطة مخول مع نقطة رجوع موثقة.

## 9. المراقبة والاستجابة

- `GET /api/v1/health` يعيد حالة ونسخة ووقت فقط.
- Request ID لكل طلب، وstructured logs منزوع منها PII.
- Workers Logs مفعلة في الإنتاج، مع تنبيه عند ارتفاع 5xx، فشل تسجيل الدخول، فشل مهمة مجدولة، أو فشل إرسال متكرر.
- واجهة المستخدم ترسل أخطاء عميل مختصرة مع الإصدار والمسار ومعرف الطلب، دون محتوى النماذج.
- runbook للحوادث: تقييم، احتواء، حفظ الأدلة، استعادة، إشعار المتأثرين بحسب السياسة، ومراجعة بعد الحادث.

## 10. المهام المجدولة

- كل 15 دقيقة: تذكيرات مستحقة وإعادة محاولة إشعارات فاشلة بحد أقصى.
- يوميًا: إبطال دعوات منتهية، تنظيف مسودات مجهولة منتهية، وفحص كائنات R2 المعلقة.
- أسبوعيًا: ملخص أخطاء تشغيلية ومساحات اقتربت من حدود التخزين.
- شهريًا: تقرير صحة النسخ والتدقيق، مع عدم تنفيذ حذف دائم واسع دون dry-run.

## 11. قائمة إطلاق أمنية

- تغيير بيانات Seed وكلمات المرور الافتراضية أو تعطيل الحسابات التجريبية.
- ضبط `BETTER_AUTH_SECRET`, `TURNSTILE_SECRET_KEY` وأي مفتاح بريد كأسرار.
- تقييد token النشر بأقل الصلاحيات.
- تشغيل اختبارات العزل وCSRF ورفع الملفات وفتح redirect.
- مراجعة CSP وCookie flags من الاستجابة الإنتاجية.
- إثبات أن R2 خاص وأن روابط الملفات غير دائمة.
- إجراء تصدير واستعادة تجريبية قبل أول ورشة حقيقية.
- نشر سياسة الخصوصية ووسيلة طلب تصحيح/حذف بيانات المشارك.

## 12. مصادر تقنية رسمية راجعها المنفذ

- Cloudflare Workers Static Assets: https://developers.cloudflare.com/workers/static-assets/
- Cloudflare D1 Time Travel: https://developers.cloudflare.com/d1/reference/time-travel/
- Cloudflare R2 Workers API: https://developers.cloudflare.com/r2/api/workers/workers-api-usage/
- Cloudflare Turnstile server validation: https://developers.cloudflare.com/turnstile/get-started/server-side-validation/
- Better Auth with Hono: https://better-auth.com/docs/integrations/hono
