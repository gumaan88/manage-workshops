# منصة ورش - حزمة مواصفات التنفيذ الكاملة

الإصدار: 1.0  
تاريخ المواصفة: 2026-09-09  
النطاق النهائي: `https://workshops.gumaan.app`

## الغرض من الحزمة

هذه الحزمة هي مرجع تعاقدي وتنفيذي لوكيل برمجي يبني منصة إدارة ورش متعددة، مكتملة وجاهزة للتجربة والعمل، وليست نموذجًا أوليًا أو واجهات استعراضية. تحتوي الحزمة على متطلبات المنتج، الأدوار والصلاحيات، تدفقات الاستخدام، نموذج البيانات، مخطط قاعدة البيانات، بيانات Seed واقعية، عقد API، ضوابط الأمن، خطة الاختبار، وتعليمة رئيسية جاهزة للوكيل.

## ترتيب القراءة والتنفيذ

1. `01_PRODUCT_SCOPE.md`: الرؤية، حدود الإصدار الأول، ومقاييس النجاح.
2. `02_FUNCTIONAL_REQUIREMENTS.md`: المتطلبات الوظيفية التفصيلية المرقمة.
3. `03_ROLES_AND_PERMISSIONS.md`: نموذج الصلاحيات ومصفوفة الوصول.
4. `04_USER_FLOWS_AND_UX.md`: الشاشات والتدفقات وتجربة العربية والجوال.
5. `05_ACTIVITY_ENGINE.md`: قلب المنصة التفاعلي وكيف تعمل الأنشطة والاستبيانات.
6. `06_DATA_MODEL.md`: شرح الكيانات وقواعد العزل والنزاهة.
7. `07_API_CONTRACT.yaml`: عقد REST قابل للتحويل إلى توثيق OpenAPI.
8. `08_SECURITY_PRIVACY_OPERATIONS.md`: الأمن والخصوصية والتشغيل والنسخ الاحتياطي.
9. `09_STACK_AND_DEPLOYMENT.md`: الاستاك، هيكل المشروع، والإطلاق على النطاق.
10. `10_TESTING_AND_ACCEPTANCE.md`: اختبارات القبول وتعريف الإنجاز.
11. `11_FUTURE_ROADMAP.md`: توسعات لاحقة لا تُخلط بالإصدار الأول.
12. `12_AGENT_MASTER_PROMPT.md`: المطالبة التي تُسلّم لوكيل البرمجة مع هذه الحزمة.
13. `database/0001_schema.sql`: مخطط SQLite/D1 المرجعي.
14. `database/0002_seed_reference.sql`: Seed مرجعي قابل للتطبيق مباشرة بعد المخطط.
15. `seed/digital_transformation_workshop.seed.json`: نسخة منظمة من محتوى الورشة الحالية.
16. `seed/scenario_library.seed.json`: حالات احتياطية للمجموعات إذا لم يقدم المشاركون مشكلات واضحة.
17. `seed/demo_completed_run.seed.json`: محاكاة منفصلة لاختبار التقارير والشهادات.
18. `13_TRACEABILITY_MATRIX.md`: ربط الوحدات بالمتطلبات واختبارات القبول.
19. `14_NON_FUNCTIONAL_REQUIREMENTS.md`: الأداء والاعتمادية والوصولية والصيانة.
20. `15_VALIDATION_REPORT.md`: نتائج التحقق من ترابط الحزمة وSeed وSQL وOpenAPI.
21. `config/wrangler.example.jsonc` و`config/env.example`: إعدادات النشر والأسرار المطلوبة.

## القرار التنفيذي

يبني الوكيل مستودعًا واحدًا TypeScript يحتوي واجهة React وAPI في Hono ويُنشر كوحدة واحدة على Cloudflare Workers. قاعدة البيانات D1، والملفات الخاصة في R2، والمصادقة Better Auth مع Drizzle. الإصدار الأول متعدد مساحات العمل والورش، عربي أولًا، متجاوب مع الجوال، قابل للتثبيت كتطبيق PWA، ويحفظ مسودات المشاركين عند ضعف الاتصال.

## ما الذي يعنيه «مكتمل»؟

- لا توجد صفحات وهمية أو أزرار بلا عمل أو بيانات ثابتة داخل الواجهة.
- جميع الوظائف الأساسية تعمل من الواجهة حتى قاعدة البيانات مع تطبيق الصلاحيات على الخادم.
- يمكن إنشاء مساحة عمل، منظمة، برنامج، ورشة، أجندة، مجموعات، نشاط أو استبيان، دعوة مشاركين، تشغيل الورشة مباشرة، استلام المخرجات، وإصدار التقرير والشهادات.
- يعمل Seed الورشة الحالية من أمر واحد، ويُنشئ بيانات قابلة للتجربة دون بيانات حساسة أو هويات مختلقة.
- تنجح اختبارات الوحدة والتكامل والنهاية إلى النهاية، وتنجح الهجرة على قاعدة فارغة.
- يعمل النشر على `workshops.gumaan.app` مع إعدادات إنتاج وفحص صحة ومراقبة وأخطاء مفهومة.

## افتراضات تحتاج قيمًا لا قرارات تصميمية

لا يحتاج الوكيل إلى سؤال المالك عن التصميم أو النطاق ليبدأ. القيم الوحيدة التي تُضبط عند النشر هي معرفات Cloudflare، أسرار المصادقة، بيانات أول مدير، وبيانات مزود البريد إن فُعّل. لا توضع أي أسرار حقيقية داخل المستودع.

## قاعدة أولوية الوثائق

عند التعارض: `10_TESTING_AND_ACCEPTANCE.md` ثم المتطلبات المرقمة ثم عقد API ثم مخطط SQL. يجب على الوكيل تسجيل أي انحراف تقني ضروري في `docs/IMPLEMENTATION_DECISIONS.md` مع سبب واختبار يثبت تكافؤ السلوك.
