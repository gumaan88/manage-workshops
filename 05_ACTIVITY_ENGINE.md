# محرك الأنشطة والاستبيانات

## 1. لماذا محرك واحد؟

الاستبيان والاختبار والتمرين الجماعي والتزام المدير كلها تشترك في: تعريف حقول، جمهور، نافذة إتاحة، إجابة، حالة، ونتيجة. يبني الإصدار الأول محركًا واحدًا قابلًا للتهيئة بدل جداول ومنطق منفصل لكل نوع. نوع النشاط يحدد الإعدادات الافتراضية والعرض والتقرير، لكنه لا ينسخ البنية.

## 2. دورة حياة النشاط

`draft → published → open → paused → closed → archived`

- `draft`: قابل للتعديل الكامل ولا يراه المشارك.
- `published`: ظاهر في الأجندة أو المتطلبات لكنه غير متاح للإجابة.
- `open`: يقبل مسودات وإرسالًا حسب القواعد.
- `paused`: يحتفظ بالحالة ولا يقبل تعديلات مؤقتًا.
- `closed`: لا يقبل إرسالًا إلا بعد إعادة فتح صريحة.
- `archived`: محفوظ للتقارير ولا يظهر في التشغيل اليومي.

أي تعديل على نوع حقل أو خياراته بعد أول إرسال ينشئ `activity_version` جديدًا. تظل الإجابات مربوطة بالإصدار الذي أجابت عليه.

## 3. أنماط الملكية

| النمط | سجل التسليم | من يحرر | الاستخدام |
|---|---|---|---|
| فردي `individual` | لكل عضو ورشة | المشارك نفسه | اختبار، قبلي/بعدي، انعكاس شخصي |
| منظمة `organization` | لكل منظمة مستهدفة | ممثلوها حسب السياسة | بطاقة فرصة أو خطة جهة |
| مجموعة `group` | لكل مجموعة نشاط | جميع الأعضاء أو المقرر | رحلة مستفيد وتمرين جماعي |
| ميسر `facilitator` | لكل مشارك/جهة/مجموعة | الميسر | تقييم Rubric أو ملاحظة متابعة |

لا ينشئ النظام تسليمًا منفصلًا لكل عضو في النمط الجماعي. يحتفظ بتاريخ التعديلات وهوية المحرر، ويستخدم `version` للتزامن المتفائل.

## 4. الحقول وقواعد التحقق

كل `activity_field` يحتوي:

- `key` ثابت داخل إصدار النشاط.
- `type` من الأنواع المحددة في `FR-071`.
- `label`, `description`, `placeholder` مترجمة.
- `required`, `position`.
- `validation_json`: طول أدنى/أقصى، رقم أدنى/أقصى، صيغة URL، عدد اختيارات، أنواع ملفات.
- `visibility_rule_json`: قاعدة واحدة أو مجموعة AND بسيطة تعتمد حقولًا سابقة؛ لا يُنفذ JavaScript محفوظ من المستخدم.
- `scoring_json`: الإجابة الصحيحة والوزن والتغذية الراجعة للاختبارات.

المصفوفة تخزن الصفوف والأعمدة كخيارات، والإجابة كخريطة `row_key → option_key`. الترتيب يخزن مصفوفة مفاتيح. الملفات تخزن `file_asset_id` لا رابطًا عامًا.

## 5. الإرسال والحفظ

- `draft`: حفظ دوري مع `If-Match` أو `version` في الجسم.
- `submitted`: تحقق جميع الحقول على الخادم وتثبيت `submitted_at`.
- `returned`: أعاده الميسر بتعليق للتعديل.
- `resubmitted`: إرسال بعد الإعادة.
- `evaluated`: اكتمل تقييم الميسر.

الحفظ idempotent باستخدام `Idempotency-Key` للطلبات النهائية. الرد يعيد `submission_id`, `status`, `version`, `saved_at`. عند إصدار قديم يعيد الخادم `409` مع نسخة الخادم وحقول التعارض دون كشف بيانات خارج النطاق.

## 6. الخصوصية

مستويات الهوية:

- `named`: الهوية ظاهرة للميسرين المخولين.
- `facilitator_hidden`: مخفية في الواجهة والتقارير اليومية، ويمكن لمسؤول مساحة مخول كشفها فقط لحادث سلامة موثق.
- `anonymous`: لا يخزن `user_id` في التسليم؛ يخزن رمز مشاركة أحادي الاستخدام لمنع التكرار. لا يدعم الاستئناف عبر أجهزة متعددة.

الاختبارات القبلية/البعدية التي تحتاج مقارنة فردية تستخدم رمز ربط عشوائي منفصل عن الهوية الظاهرة، وتعرض النتائج مجمعة فقط إذا اختيرت الخصوصية المجهولة.

## 7. التقييم والدرجات

- الدرجة الآلية للاختبار تحسب من الأوزان وتُخزن مع إصدار قواعد التصحيح.
- Rubric يحتوي معايير، لكل معيار وزن ومستويات بقيم ووصف.
- الدرجة النهائية = مجموع `(قيمة المستوى / أعلى قيمة) × وزن المعيار`.
- يمكن إضافة تعليق خاص للمشارك وتعليق داخلي للميسرين.
- إعادة التقييم تنشئ مراجعة ولا تمحو القديمة؛ التقرير يستخدم أحدث مراجعة معتمدة.

## 8. التشغيل والعرض المباشر

- حالة الورشة الحية سجل واحد مع `state_version` متزايد.
- العميل يطلب `/live-state?sinceVersion=n` كل 3 ثوانٍ أثناء الورشة و15 ثانية في الخلفية.
- إذا لم تتغير الحالة يعيد الخادم `204` أو `304`.
- نتائج التصويت تعرض أعدادًا ونسبًا بعد تحقق عتبة الخصوصية.
- مخرجات النص لا تظهر تلقائيًا على شاشة العرض؛ يختار الميسر عناصر محددة.

هذا التصميم يلبي حجم الورش المعتاد بتكلفة وتعقيد أقل. Durable Objects/WebSockets توسعة لاحقة إذا أثبت القياس الحاجة إلى تزامن دون الثانية أو مئات المتصلين في ورشة واحدة.

## 9. أنواع الأنشطة الجاهزة في القالب

1. تقييم قبلي/بعدي للقناعة والمعرفة.
2. سؤال افتتاحي أو Word cloud نصي (يعرض بعد مراجعة الميسر).
3. خريطة رحلة مستفيد جماعية.
4. سلسلة المشكلة والأثر.
5. بطاقة فرصة رقمية أولية لكل منظمة.
6. تصويت أولوية.
7. Rubric تقييم مخرج.
8. التزام قيادي وخطوة خلال 30 يومًا.
9. تقييم الورشة ورضا المشارك.

## 10. التقارير المشتقة

لا تحفظ النسب والمجاميع كحقائق دائمة إلا كنسخة تقرير معتمدة. تُحسب من الإجابات والتقييمات مع فلاتر واضحة. أي رقم يعرض معه المقام وعدد القيم المفقودة. لا يُحسب المتوسط لمقياس نصي، ولا تُقارن أسئلة قبلية/بعدية ما لم تحمل `comparison_key` نفسه ونفس المقياس.
