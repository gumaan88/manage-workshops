# نموذج البيانات وقواعد النزاهة

## 1. اصطلاحات عامة

- جميع المعرفات `TEXT` وتولد في التطبيق بصيغة UUIDv7 أو CUID2؛ لا تستخدم أرقامًا متسلسلة في الروابط.
- جميع الأوقات محفوظة UTC بصيغة ISO-8601، وتُعرض حسب `timezone` للورشة أو المساحة.
- الحقول متعددة اللغة تحفظ كنص JSON مثل `{"ar":"...","en":"..."}` عندما يلزم، بينما الحقول الإدارية الداخلية العربية يمكن أن تكون نصًا مباشرًا.
- كل جدول تشغيلي تابع لمساحة يحمل `workspace_id` مباشرة حتى يسهل فرض العزل والفهرسة، حتى إن أمكن استنتاجه من علاقة أخرى.
- الحذف التاريخي منطقي باستخدام `archived_at` أو `deleted_at`. المخرجات والحضور والتدقيق لا تحذف بالحذف العادي.
- يستخدم Drizzle لتعريف المخطط في الكود، لكن ملفات SQL في هذه الحزمة هي المرجع السلوكي ويجب أن تنتج عنها هجرة مكافئة.

## 2. خريطة الكيانات

| المجال | الكيانات الأساسية | العلاقة |
|---|---|---|
| المصادقة | `users`, `sessions`, `accounts`, `verifications` | متوافقة مع Better Auth وتُمدد دون تعديل مكتبة المصادقة يدويًا |
| الملكية | `workspaces`, `workspace_memberships` | المستخدم عضو في مساحات متعددة ودور واحد أساسي في كل مساحة |
| العملاء | `organizations`, `organization_memberships` | المنظمة داخل مساحة، والمستخدم قد يمثل أكثر من منظمة |
| التنظيم | `programs`, `workshops`, `workshop_memberships` | البرنامج يضم ورشًا؛ عضوية الورشة تحدد الدور والمنظمة الممثلة |
| التصميم | `workshop_days`, `agenda_items`, `workshop_objectives`, `workshop_axes` | مكونات مرتبة للورشة |
| المشاركة | `invitations`, `registration_fields`, `registration_responses`, `attendance_records` | الدعوة والتسجيل والحضور |
| المجموعات | `workshop_groups`, `group_memberships` | مجموعة افتراضية أو مرتبطة بنشاط |
| الأنشطة | `activities`, `activity_versions`, `activity_fields`, `activity_options`, `activity_targets` | تعريف قابل للإصدار والاستهداف |
| المخرجات | `submissions`, `submission_answers`, `evaluations`, `evaluation_scores` | تسليم فرد/منظمة/مجموعة وتقييمه |
| الملفات | `file_assets`, `submission_files`, `materials` | البيانات الوصفية في D1 والمحتوى الخاص في R2 |
| التشغيل | `live_states`, `live_events`, `notifications` | الحالة الحالية والأحداث والرسائل |
| النتائج | `report_snapshots`, `certificate_templates`, `certificates` | تقارير معتمدة وشهادات قابلة للتحقق |
| الحوكمة | `audit_logs`, `idempotency_keys`, `support_sessions` | تدقيق وحماية من التكرار ودعم مضبوط |
| القوالب | `workshop_templates` | Blueprint JSON بإصدار، بلا بيانات تشغيلية |

## 3. قواعد العزل

1. Middleware يستخرج المستخدم والجلسة.
2. كل route تحت `/workspaces/:workspaceId` يتحقق من عضوية نشطة.
3. الخدمة تمرر `workspaceId` الموثوق إلى repository؛ لا توجد دالة repository لقراءة مورد تابع لمساحة بالـID فقط.
4. عند علاقة بين كيانين، يتحقق الخادم أن كليهما في المساحة نفسها قبل الكتابة.
5. الملفات تستخدم مفتاح R2 يبدأ بمعرف المساحة، لكن الوصول دائمًا عبر Worker بعد تحقق D1.
6. التقارير والتصدير تستخدم نفس طبقة الصلاحيات ولا تنفذ SQL حرًا قادمًا من العميل.

## 4. قواعد نزاهة مهمة

- `workspace_memberships`: عضوية واحدة للمستخدم في المساحة، ولا تُحذف عضوية آخر مالك.
- `organizations`: الاسم لا يُفرض فريدًا عالميًا؛ يستخدم `normalized_name` للكشف داخل المساحة مع السماح باستثناء موثق.
- `workshops`: `end_at > start_at` و`capacity` موجب إن وُجد.
- `workshop_memberships`: مستخدم واحد مرة واحدة في الورشة؛ `organization_id` يجب أن يكون من المساحة نفسها.
- `agenda_items`: البداية والنهاية داخل اليوم؛ فحص التداخل في الخدمة لأن بعض الحالات تسمح بتحذير وتجاوز.
- `activities`: النوع والملكية والخصوصية محكومة بقيم ثابتة.
- `activity_fields`: `field_key` فريد داخل إصدار النشاط.
- `submissions`: مفتاح فريد حسب النشاط وإصداره ومالك الإجابة؛ يُنفذ بواسطة `owner_key` موحد لمنع قيود NULL المركبة.
- `submission_answers`: إجابة واحدة لكل حقل في التسليم؛ القيمة JSON أو نص حسب النوع، وتتحقق الخدمة من البنية.
- `certificates.public_code`: عشوائي فريد، وليس ID الشهادة.
- `audit_logs`: لا توجد endpoints للتعديل أو الحذف.

## 5. الأنشطة ذات الإصدارات

`activities` يحمل الهوية والحالة والاستهداف العام. `activity_versions` يحمل نسخة التعليمات والإعدادات. `activity_fields` و`activity_options` تتبع الإصدار. عند أول تسليم يصبح الإصدار immutable. تعديل بنيوي ينسخ الإصدار ويزيد `version_no` ثم يغير `current_version_id`.

التسليم يرتبط بكل من `activity_id` و`activity_version_id`. لذلك لا تتغير دلالة إجابة قديمة عند تعديل سؤال لاحقًا.

## 6. `owner_key` للتسليم

لتوحيد أنماط الملكية ومنع التكرار:

- فرد: `user:<user_id>`
- منظمة: `organization:<organization_id>`
- مجموعة: `group:<group_id>`
- مجهول: `anonymous:<random_participation_token_hash>`

الخادم يبني المفتاح ولا يقبل قيمة جاهزة من العميل. الأعمدة المنفصلة (`owner_user_id`, `owner_organization_id`, `owner_group_id`) تبقى للاستعلام والتكامل المرجعي.

## 7. البيانات المحسوبة

- نسبة الحضور، الإكمال، الدرجات المجمعة، والفرق القبلي/البعدي تُحسب عند الطلب أو في snapshot تقرير معتمد.
- `report_snapshots` يخزن JSON البيانات المستخدمة وقت الاعتماد إضافة إلى السرد، حتى لا تتغير نسخة تقرير سابقة عند تصحيح لاحق.
- `live_states.state_version` يزداد ذريًا في كل تغيير ويسمح بتحديث العملاء بكفاءة.

## 8. البيانات الحساسة

المنصة لإدارة الورش، وليست نظام إدارة حالات مستفيدين. يمنع تصميم النماذج الافتراضي إدخال أسماء مستفيدين أو سجلات صحية أو وثائق هوية في التمارين. تظهر تعليمات إخفاء الهوية في الأنشطة النصية والملفات. إذا أضاف مدير المساحة حقلًا حساسًا، يجب وسمه `sensitive=true` لتقييد العرض والتصدير والتسجيل في السجلات.

## 9. الترحيلات

- كل تغيير مخطط في ملف migration جديد؛ لا تعدل migration مطبقًا.
- CI يطبق جميع الترحيلات على D1 محلية فارغة ثم يشغل Seed واختبارات smoke.
- ترحيل الإنتاج يسبق نشر الكود المتطلب له، ويكون backward-compatible عند الإمكان.
- العمليات غير القابلة للرجوع تتطلب تصديرًا واختبار استعادة موثقًا.
