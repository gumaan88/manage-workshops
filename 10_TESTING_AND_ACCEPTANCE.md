# الاختبارات ومعايير القبول وتعريف الإنجاز

## 1. تعريف الإنجاز العام

لا تُعد المنصة منجزة لأن الصفحات موجودة. تُعد منجزة عندما تنجح المتطلبات من واجهة المستخدم حتى التخزين، وتثبت الصلاحيات والاختبارات والنشر ذلك. ممنوع في التسليم النهائي: `TODO` في مسار أساسي، بيانات hardcoded في شاشة تشغيل، أزرار بلا handler، API وهمي، اعتماد على localStorage كمصدر حقيقة، أو صفحات مكتوب عليها «قريبًا» لوظيفة ضمن النطاق.

## 2. اختبارات قبول حرجة

### `AC-001` عزل مساحة العمل

أنشئ مساحتين ومستخدمًا في كل منهما. حاول المستخدم A قراءة أو تعديل ورشة/منظمة/ملف للمساحة B عبر تبديل ID في URL وAPI. النتيجة: `404` أو `403` ثابت بلا تسريب الاسم أو وجود المورد، ولا يظهر في البحث أو التصدير.

### `AC-002` إنشاء ورشة كاملة

ينشئ مدير برنامج ورشة من الصفر، يضيف يومًا وأجندة وهدفين ومحورين وميسرًا ونشاطًا، يعاينها، ويفتح التسجيل. تظهر للمشارك بعد دعوته دون تدخل قاعدة بيانات يدوي.

### `AC-003` نسخ قالب

تحويل ورشة إلى قالب ثم إنشاء ورشة جديدة منه. تنتقل الأهداف والأجندة والأنشطة ولا ينتقل أي مشارك أو حضور أو إجابة. تعديل النسخة لا يغير القالب أو الأصل.

### `AC-004` التسجيل والدعوة

ينشئ المنسق رابط دعوة محدود الاستخدام، يسجل مستخدم جديد، يقبل الشروط، ويصبح في حالة الاعتماد الصحيحة. إعادة استخدام token بعد الحد تفشل برسالة مناسبة. التسجيل المكرر لا ينشئ عضويتين.

### `AC-005` الحضور

يمسح المنسق QR صحيحًا فيسجل حضور اليوم مرة واحدة. مسح مكرر يعرض أنه مسجل ولا يضاعف السجل. تعديل يدوي يحتاج سببًا ويظهر في التدقيق.

### `AC-006` نشاط فردي مع ضعف الاتصال

يفتح المشارك نشاطًا، يكتب إجابة، يفصل الشبكة، يعدل، ثم يعيد الاتصال. تُزامن المسودة ويظهر وقت الخادم. الإرسال النهائي ينتج تسليمًا واحدًا حتى مع إعادة الطلب.

### `AC-007` نشاط جماعي

أعضاء مجموعة واحدة يحررون تسليمًا واحدًا. عند تعديل متزامن لا يفقد أحدهم بياناته صامتًا؛ يحصل العميل الأقدم على `409` ومسار حل. مجموعة أخرى لا تقرأ التسليم قبل السماح بالعرض.

### `AC-008` النشاط المباشر

يفتح الميسر النشاط من شاشة live. خلال 5 ثوانٍ يظهر للمشاركين. يرى الميسر أعداد الحالات الصحيحة. يغلقه فتُرفض محاولة جديدة، بينما تبقى الإجابات المحفوظة.

### `AC-009` المجهولية

استبيان مجهول لا يخزن user_id في submission ولا يظهر mapping في endpoint أو export. لا تظهر نتيجة مجموعة أقل من عتبة الخصوصية الافتراضية.

### `AC-010` القبلي/البعدي

ترتبط أسئلة بنفس `comparison_key`. التقرير يحسب العدد والمتوسط والفرق والمفقودات بصورة صحيحة، ولا يقارن مقياسين مختلفين.

### `AC-011` التقرير

بعد إكمال Seed، يولد تقرير عربي قابل للطباعة يشمل الأجندة والحضور والمشاركة والمخرجات والقبلي/البعدي والسرد. الأرقام تطابق استعلامات المصدر، والفلتر حسب المنظمة يعمل.

### `AC-012` الشهادة

مشارك يحقق المعيار تصدر له شهادة واحدة. رابط التحقق العام يثبتها ولا يعرض البريد. بعد الإلغاء تظهر «ملغاة» مع بقاء سجل التدقيق.

### `AC-013` الرفع

PDF صالح ضمن الحد يُرفع ويُنزل فقط بواسطة مخول. ملف تنفيذي مغير الامتداد يُرفض. مستخدم مساحة أخرى لا يصل للكائن حتى إن عرف المفتاح.

### `AC-014` الاستعادة والSeed

تطبق migrations وSeed على قاعدة فارغة بلا أخطاء. تشغيل Seed مرة ثانية لا يكرر السجلات. تصدير قاعدة الاختبار ثم استعادتها يعيد counts والعلاقات نفسها.

### `AC-015` العربية والوصولية

مسارات الإدارة والمشارك RTL بلا قص أو تمرير أفقي على عرض 360px في التدفقات الأساسية. كل نموذج قابل للوحة المفاتيح، الأخطاء مرتبطة بالحقول، والتباين AA.

## 3. تغطية الاختبارات

### وحدة

- انتقالات حالة الورشة والنشاط.
- حساب الدرجات والـRubric والقبلي/البعدي.
- بناء `owner_key`.
- صلاحيات الأدوار.
- قواعد الدعوات والانتهاء وحد الاستخدام.
- إخفاء الهوية وعتبة الخصوصية.
- parsing/validation لكل نوع حقل.

### تكامل Worker + D1/R2

- المصادقة والجلسة وCSRF.
- CRUD مع workspace scoping.
- معاملات إنشاء التسليم والإجابات.
- idempotency والتعارض.
- upload metadata والتنزيل المصرح.
- migrations وseed.

### نهاية إلى نهاية

- رحلة مدير ينشئ ورشة وينشرها.
- رحلة منسق يدعو ويسجل حضورًا.
- رحلة مشارك فردي وجماعي على viewport هاتف.
- رحلة ميسر live ثم تقرير وشهادة.
- حالات 401/403/404/409/422/429/500 وفقد الشبكة.

## 4. متصفحات وأجهزة

- آخر نسختين مستقرتين من Chrome وEdge وFirefox وSafari.
- Android Chrome وiOS Safari حديثان.
- قياسات مرجعية: 360×800، 390×844، 768×1024، 1366×768.
- لا يلزم دعم Internet Explorer.

## 5. بوابات الجودة قبل التسليم

- typecheck بلا أخطاء.
- lint وformat check ناجحان.
- لا ثغرات حرجة أو عالية معروفة في dependency audit دون استثناء موثق.
- جميع اختبارات القبول الحرجة ناجحة.
- 0 أخطاء console غير متوقعة في E2E.
- جميع migrations قابلة للتطبيق على قاعدة فارغة وعلى نسخة الإصدار السابق إن وجدت.
- فحص bundle والأداء؛ لا يحمل المشارك وحدات الإدارة الثقيلة في أول صفحة.
- فحص secrets؛ لا مفاتيح أو كلمات مرور أو ملفات `.dev.vars` في git.

## 6. التسليم المطلوب من وكيل البرمجة

- مستودع المصدر كاملًا مع lockfile.
- README تشغيل ونشر واستكشاف أعطال.
- migrations وseed idempotent.
- توثيق API مولد ومتاح في بيئة التطوير للمخولين.
- حسابات تجريبية تنشأ من environment لا كلمات ثابتة في المستودع.
- تقرير اختبارات يتضمن الأوامر والنتائج.
- قائمة قرارات وانحرافات تنفيذية.
- runbook للنسخ والاستعادة والدعم والحوادث.
- رابط preview يعمل، ثم تعليمات/تنفيذ ربط `workshops.gumaan.app` حسب الصلاحية المتاحة.

## 7. قائمة منع التسليم الناقص

يرفض التسليم إذا تحقق أي مما يلي:

- CRUD يعمل في الواجهة لكن لا يستمر بعد التحديث.
- دور مخفي بصريًا لكنه يستطيع استدعاء endpoint ممنوع.
- Seed لا ينشئ الورشة المرجعية كاملة.
- نشاط جماعي ينشئ إجابة لكل شخص بدل مخرج مجموعة.
- التقرير أرقام ثابتة أو لا يوضح المقام.
- ملفات R2 عامة.
- تسجيل عام بلا تحقق Turnstile من الخادم في الإنتاج.
- لا توجد تجربة فعلية على الهاتف أو RTL.
- النشر يعتمد خطوات غير موثقة أو تعديل يدوي في الكود.
