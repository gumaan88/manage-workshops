# المتطلبات غير الوظيفية

## الأداء والسعة

- `NFR-001` يخدم النظام 200 مستخدم متزامن في ورشة واحدة و1,000 جلسة نشطة موزعة على ورش متعددة دون أخطاء وظيفية في اختبار الحمل المرجعي.
- `NFR-002` تحقق صفحات المشاركين LCP مستهدفًا أقل من 2.5 ثانية على 4G وهاتف متوسط، ولوحات الإدارة أقل من 3.5 ثانية بعد الدخول، ويُسجل القياس الفعلي.
- `NFR-003` زمن p95 لعمليات القراءة الشائعة أقل من 500ms، والكتابة الشائعة أقل من 800ms في الإنتاج المعتاد، باستثناء الرفع والتصدير.
- `NFR-004` لا يحمل أي endpoint أكثر من 100 سجل بلا pagination، ولا تنفذ تقارير واسعة بلا فلاتر وفهارس.
- `NFR-005` تظهر حالة live الجديدة خلال 5 ثوانٍ على الأقل لـ95% من العملاء المتصلين طبيعيًا.

## الاعتمادية والنزاهة

- `NFR-010` كل عملية حساسة متعددة الجداول تستخدم batch/transaction متاحة أو تعويضًا موثقًا؛ لا تترك تسليمًا بلا إجابات متسقة.
- `NFR-011` العمليات القابلة للتكرار تستخدم idempotency ولا تنشئ دعوات أو إرسالًا أو شهادات مكررة.
- `NFR-012` التعارضات لا تستخدم last-write-wins صامتًا للمحتوى؛ تعيد `409` مع مسار استعادة.
- `NFR-013` هدف التوفر التشغيلي 99.5% شهريًا باستثناء صيانة معلنة واعتماديات Cloudflare الخارجة عن السيطرة، ويُقاس من health check خارجي عند التشغيل الفعلي.
- `NFR-014` لا يفقد الانقطاع القصير مسودة نصية محفوظة محليًا، ويثبت اختبار AC-006 ذلك.

## الأمن والخصوصية

- `NFR-020` يلتزم التنفيذ بضوابط `08_SECURITY_PRIVACY_OPERATIONS.md` وبمبدأ أقل صلاحية.
- `NFR-021` لا يوجد سر أو token أو password أو PII في Git أو logs أو رسائل خطأ عامة.
- `NFR-022` كل endpoint تابع لمساحة لديه اختبار عزل إيجابي وسلبي.
- `NFR-023` ملفات R2 خاصة وتخضع للتفويض في كل تنزيل.
- `NFR-024` البيانات المجهولة لا يعاد تعريفها عبر الفلاتر أو التصدير، والعتبة الافتراضية 5.

## الوصولية واللغة

- `NFR-030` تحقق التدفقات الحرجة WCAG 2.2 AA: تباين، لوحة مفاتيح، focus، labels، وإعلانات أخطاء.
- `NFR-031` العربية وRTL هما الافتراضيان، ولا تستخدم الواجهة نصوصًا عربية hardcoded خارج ملفات الترجمة إلا المحتوى الذي أدخله المستخدم.
- `NFR-032` تخزن الأوقات UTC وتعرض حسب المنطقة الزمنية، وتبقى تواريخ التقرير مفهومة بالعربية.
- `NFR-033` تعمل التدفقات الحرجة على عرض 360px دون فقد وظيفة أو تمرير أفقي للصفحة.

## القابلية للصيانة

- `NFR-040` TypeScript strict بلا `any` غير مبرر في domain/contracts.
- `NFR-041` مصدر تحقق واحد لكل request/response يولد منه Zod/OpenAPI/أنواع العميل قدر الإمكان.
- `NFR-042` طبقات route/domain/repository منفصلة، ولا يوجد SQL في React أو route handlers الكبيرة.
- `NFR-043` migrations تراكمية، وSeed idempotent، وlockfile مثبت.
- `NFR-044` README يمكّن مطورًا جديدًا من تشغيل التطبيق والاختبارات محليًا خلال 30 دقيقة بعد توفر المتطلبات.
- `NFR-045` أي feature flag له مالك وسبب وتاريخ مراجعة؛ لا يبقى مسار أساسي معطلًا في الإنتاج.

## الرصد والدعم

- `NFR-050` لكل طلب `requestId` يظهر في log والخطأ غير المتوقع.
- `NFR-051` logs منظمة وتحتوي الزمن والبيئة والمسار والحالة والمدة، ولا تحتوي أجسام الأنشطة.
- `NFR-052` توجد runbooks للنشر والرجوع والنسخ والاستعادة وفشل البريد وحادث تسريب صلاحية.
- `NFR-053` يعرف الإصدار المنشور عبر commit SHA أو version metadata دون كشف أسرار.

## التوافق والنشر

- `NFR-060` يدعم المتصفحات المحددة في خطة الاختبار ويعرض رسالة واضحة للمتصفح غير المدعوم.
- `NFR-061` النشر قابل للتكرار من CI، ولا يعتمد تعديل ملفات يدويًا داخل Dashboard عدا ربط الأسرار والموارد أول مرة.
- `NFR-062` preview والإنتاج يستخدمان قواعد وR2 وأسرارًا منفصلة.
- `NFR-063` rollback موثق ومختبر، وترحيلات قاعدة البيانات تراعي توافق الإصدار السابق عند النشر التدريجي.
