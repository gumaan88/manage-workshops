# المطالبة الرئيسية لوكيل البرمجة

انسخ النص التالي إلى الوكيل وأرفق معه مجلد هذه الحزمة كاملًا:

---

أنت الوكيل الهندسي المسؤول عن بناء وإطلاق «منصة ورش» على النطاق النهائي:

`https://workshops.gumaan.app`

ابنِ منصة إدارة ورش متعددة مكتملة وجاهزة للتجربة والعمل الفعلي، وليست Prototype أو Demo بصريًا. الملفات المرفقة هي مواصفة المنتج الملزمة. اقرأها كلها قبل كتابة الكود بالترتيب الوارد في `00_README.md`، ثم أنشئ خطة تنفيذ قابلة للتتبع تربط كل متطلب `FR-*` باختبار وقسم كود.

## النتيجة المطلوبة

مستودع TypeScript كامل يعمل محليًا ويُنشر على Cloudflare، يحتوي:

- React/Vite عربية RTL ومتجاوبة وPWA.
- Hono API على Cloudflare Workers.
- D1 + Drizzle migrations.
- R2 خاص للملفات.
- Better Auth للمصادقة مع جلسات Cookie آمنة.
- RBAC وعزل حقيقي بين مساحات العمل.
- إدارة المستخدمين والمنظمات والبرامج والورش والأجندات والمجموعات.
- منشئ أنشطة واستبيانات واختبارات وتمارين فردية وجماعية.
- غرفة مشارك ولوحة تشغيل مباشر للميسر.
- حضور ودعوات وتسجيل وQR.
- تقارير وتصدير وصفحات طباعة وشهادات تحقق.
- إشعارات داخلية ومزود بريد قابل للتهيئة.
- سجل تدقيق ومهام تشغيل وhealth endpoint.
- Seed كامل للورشة المرجعية و22 منظمة وخمس مجموعات.
- Seed محاكاة مكتملة منفصلة لإظهار التقارير والشهادات في preview فقط.
- اختبارات وحدة وتكامل وE2E ناجحة.

## قواعد غير قابلة للتفاوض

1. لا تسلّم صفحات placeholder أو بيانات hardcoded أو أزرارًا غير عاملة أو `TODO` لمسار داخل النطاق.
2. لا تعتبر إخفاء الزر صلاحية؛ طبق كل permission على الخادم واختبر IDOR بين مساحتين.
3. لا تكتب نظام مصادقة من الصفر؛ استخدم Better Auth وDrizzle وفق أحدث توثيق رسمي متوافق.
4. لا تجعل R2 عامًا، ولا تضع أسرارًا أو كلمات مرور حقيقية في Git.
5. لا تستخدم localStorage كمصدر حقيقة. استخدم IndexedDB فقط لمسودات offline المؤقتة مع مزامنة مؤكدة.
6. لا تغيّر معنى Seed الورشة الحالية: هي ورشة وعي قيادي أربع ساعات عن «لماذا التحول الرقمي؟ وأثره على المستفيد»، وليست تقييم جاهزية شاملًا أو خطة تحول تفصيلية في هذه المرحلة.
7. استخدم أحدث نسخ مستقرة متوافقة عند بدء البناء وثبتها في lockfile. لا تعتمد أرقام إصدارات متخيلة.
8. إذا وجدت تعارضًا، طبق قاعدة الأولوية في `00_README.md`. سجل أي انحراف ضروري في `docs/IMPLEMENTATION_DECISIONS.md` مع السبب والبديل والاختبار.
9. كل migration وSeed يجب أن يكونا قابلين للتكرار حسب التعليمات، ويجب أن تنجح قاعدة فارغة من أمر واحد.
10. لا تدّع اكتمال النشر إن لم تختبر الرابط وhealth ومسارات الدخول فعليًا.

## أسلوب التنفيذ

1. افحص بيئة المستودع الحالية وملفات `AGENTS.md` إن وجدت واحفظ تغييرات المستخدم القائمة.
2. أنشئ مصفوفة تتبع: المتطلب، route/API، الشاشة، اختبار القبول، الحالة.
3. ثبّت skeleton كاملًا، ثم طبقة البيانات والمصادقة والتفويض قبل الواجهات التفصيلية.
4. نفّذ vertical slices قابلة للاختبار: مساحة → ورشة → دعوة → نشاط → live → تقرير، ثم أكمل بقية المتطلبات.
5. استخدم Zod schemas مشتركة ومدخلات غير موثوقة دائمًا.
6. أنشئ OpenAPI من العقود واجعل `/api/docs` متاحًا في local/preview أو لمستخدم مخول فقط في الإنتاج.
7. أنشئ Seed idempotent. المستخدم الإداري يُنشأ من `SEED_ADMIN_EMAIL` و`SEED_ADMIN_PASSWORD` ولا يوضع password hash ثابت في SQL.
8. اختبر viewport هاتف عربي منذ أول slice، وليس في النهاية.
9. نفّذ اختبارات `AC-001` إلى `AC-015` حرفيًا وأرفق النتائج.
10. شغّل build والنشر التجريبي ثم production فقط إذا كانت صلاحيات Cloudflare والنطاق متاحة.

## التعامل مع النشر والأسرار

القيم الوحيدة التي يجوز طلبها من المالك عند الحاجة هي:

- Cloudflare Account/Zone access أو API token محدود للنشر.
- معرفات D1 وR2 إن لم يسمح لك بإنشائها.
- `BETTER_AUTH_SECRET` أو إذن توليده وضبطه.
- مفاتيح Turnstile.
- بيانات مزود البريد الاختياري.
- بريد وكلمة مرور أول مدير، وتُمرر كأسرار بيئة.

إذا لم تتوفر صلاحية النشر، أكمل التطبيق والاختبارات وpreview المحلي وقدم أوامر دقيقة للنشر؛ لا تستبدل ذلك بادعاء أن الإنتاج جاهز. لا تطلب قرارات تصميمية حُسمت في الملفات.

## متطلبات التسليم النهائي

- تقرير مختصر يبدأ بما تم فعليًا وما لم يمكن بسبب صلاحية خارجية.
- رابط preview والإنتاج إن تم نشرهما.
- حساب تجربة يُنشأ عبر القيم السرية، مع تعليمات تغيير كلمة المرور.
- نتائج الأوامر: typecheck, lint, unit, integration, e2e, build, migrations, seed.
- قائمة الملفات الرئيسة والمخطط المعماري المختصر.
- طريقة النسخ والاستعادة وتدوير الأسرار ومعالجة فشل البريد.
- صور أو تسجيل قصير للتدفقات الحرجة على الهاتف وسطح المكتب إن كانت أدوات البيئة تسمح.
- لا تعلن «تم» قبل اجتياز `10_TESTING_AND_ACCEPTANCE.md` كاملًا.

ابدأ بقراءة الحزمة وإخراج خطة ومصفوفة تتبع، ثم نفذ المشروع كاملًا باستقلالية. توقف فقط عند حاجة فعلية إلى صلاحية خارجية أو سر، واطلب القيمة المحددة دون توسيع نطاق الوصول.

---

## ملاحظة لصاحب المشروع

يفضل تسليم الوكيل الحزمة كما هي وعدم اختصار المطالبة وحدها؛ التفاصيل الملزمة موجودة في الملفات المرتبطة، والمطالبة تمنع أن يتحول التنفيذ إلى نموذج أولي ناقص.
